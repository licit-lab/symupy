version: 2

sphinx:
  builder: html
  configuration: docs/source/conf.py

conda:
  environment: docs/environment_doc.yml

python:
  version: 3.8
  install:
    # install symupy itself
    - method: pip
      path: .