version: 2
sphinx:
  builder: html
  configuration: docs/source/conf.py
python:
  version: 3.7
  install:
    - requirements: requirements_dev.txt
    - method: pip
      path: .
      extra_requirements:
        - docs
    - method: setuptools
      path: .
    # install docs requirements
